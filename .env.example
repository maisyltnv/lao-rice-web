# Local dev example (copy to `.env.local`).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8081

# Production example (set as env var on server before build, or use `.env.production`).
# NEXT_PUBLIC_API_URL=https://api.khaosan.online
